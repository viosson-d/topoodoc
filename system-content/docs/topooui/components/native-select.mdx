---
title: "Native Select"
description: "A styled native HTML select element with consistent design system integration."
---
A styled native HTML select element with consistent design system integration.

## Installation

```bash
npx shadcn@latest add native-select
```

## Usage

```tsx
// Native Select preview page used while the component-specific preview runtime is still being wired in.
```

## Notes

This page is now part of the TopooUI component reference board. The remaining work is to replace the placeholder examples with the official live preview blocks.
